# Load the package under test from the installed library.
# testthat sets the working directory to tests/testthat/ so paths relative to
# that are used in golden() inside the test files.
library(picklerick)
